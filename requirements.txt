anthropic>=0.39.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
playwright>=1.42.0
apscheduler>=3.10.4
sqlmodel>=0.0.16
python-dotenv>=1.0.1
pydantic>=2.6.0
pydantic-settings>=2.2.1
jinja2>=3.1.3
python-multipart>=0.0.9
# v2 additions: trend scout uses stdlib only (no extra deps).
# Replicate API uses stdlib urllib (no `replicate` SDK dependency).
